paths:
  /users/logout:
    get:
      tags:
        - user
      summary: logout the user from all sessions.
      produces:
        - application/json
      responses:
        200:
          description: The user logged out successfully.
        401:
          description: can't logout the user.
